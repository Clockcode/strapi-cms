{
  "routes": [
    {
      "method": "GET",
      "path": "/menu-other-pages",
      "handler": "menu-other-pages.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/menu-other-pages/count",
      "handler": "menu-other-pages.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/menu-other-pages/:id",
      "handler": "menu-other-pages.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/menu-other-pages",
      "handler": "menu-other-pages.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/menu-other-pages/:id",
      "handler": "menu-other-pages.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/menu-other-pages/:id",
      "handler": "menu-other-pages.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
